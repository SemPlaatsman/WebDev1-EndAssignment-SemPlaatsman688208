// function setAddMemberButton(text = "Add member", bootstrapClass = "btn-primary") {
//     const addMemberButton = document.getElementById('add-member-button');
//     addMemberButton.textContent(text);
//     addMemberButton.classList.add(bootstrapClass);
// }

setTimeout(() => {
    document.location.reload();
}, 5000);